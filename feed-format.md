# feed.md 生成形式

このファイルは、Claude CLI に `feed.md` の出力形式を伝えるための仕様です。
`raw/*.json` は生成済みである前提で読み込み、`feed.md` 全体を上書きしてください。

## 実行範囲

- `raw/*.json` を読み、技術ニュース要約として `feed.md` を生成する。
- `feed.md` は必ず全体を上書きする。
- fetch、commit、push、メール送信は行わない。

## 出力形式

```markdown
# 技術ニュース要約 — YYYY-MM-DD

## 📌 今日の3行サマリ

- <今日最も注目すべき技術ニュースを1行で>
- <2 件目>
- <3 件目>

## GitHub Trending

| # | タイトル | 要約 | URL |
|---|----------|------|-----|
| 1 | <日本語見出し> | • 要点1<br>• 要点2<br>• 要点3<br><br>1段落で背景/影響/今後をまとめる。 | <一次ソース URL> |

## Hacker News
（同様のテーブル形式。英語ソースは見出しを「<日本語意訳> — (原文: <English Title>)」形式で併記。）

## Anthropic
（同様。英語ソース。）

## OpenAI
（同様。英語ソース。）

## InfoQ Japan
（同様。日本語ソースの場合は日本語意訳のみ。）

## はてなブックマーク (tech)
（同様。日本語ソース。）

## Zenn
（同様。）

## Qiita
（同様。）
```

## セクションルール

- セクション順は GitHub Trending、Hacker News、Anthropic、OpenAI、InfoQ Japan、はてなブックマーク (tech)、Zenn、Qiita とする。
- **📌 今日の3行サマリ**: 全ソース横断で最も注目度の高い 3 件を 1 行ずつ。メール開封時に要点が伝わるよう、平易な日本語で。
- **英語圏ソース (Anthropic / OpenAI / InfoQ Japan / Hacker News)**: タイトル列は「<日本語意訳> — (原文: <English Title>)」形式で原題を併記する。
- **日本語ソース (Zenn / Qiita / はてな / InfoQ Japan)**: タイトル列は日本語意訳のみ。原文が長すぎる場合は適宜省略。
- 取得できなかったソース、または有用な項目がないソースは見出しごと省略してよい。
- 各ソースから、注目度(score)の高いものを中心に 5〜8 件ずつ選ぶ。
- 各記事は「日本語見出し」「要約(箇条書き3点+1段落)」「一次ソース URL」の順に書く。
- URL は各項目の末尾に単独行で置く。

## 要約ルール

1. **タイトル**: 内容がわかる日本語見出しにする。英語ソースは「<日本語意訳> — (原文: <English Title>)」形式で原題を併記。
2. **要約フォーマット** (重要): 必ず以下の構造にする:
   - 箇条書き 3 点 (各 1 行、`• ` で始める、改行は `<br>`)
   - 続いて 1 段落で背景・影響・今後をまとめる
   - 形式: `• 要点1<br>• 要点2<br>• 要点3<br><br>まとめの段落。`
3. **文字数**: 箇条書き 3 点 + 1 段落で合計 4〜5 文相当。
4. **一次ソース URL**: raw の URL を優先。
5. **客観的・中立**: 誇張、煽り、断定しすぎる表現を避ける。
6. **raw の `extra`**: タグ、コメント数、説明文などは要約の判断材料として使ってよい。
