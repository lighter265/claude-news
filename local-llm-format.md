# feed-local-llm.md 生成形式

このファイルは、Claude CLI に `feed-local-llm.md` の出力形式を伝えるための仕様です。
`raw/local_llm_github.json` と `raw/reddit_local_llm.json` は生成済みである前提で読み込み、
`feed-local-llm.md` 全体を上書きしてください。どちらか一方が空配列でもエラーにせず、
取得できた側のセクションを生成してください。

## 実行範囲

- `raw/local_llm_github.json` を読み、GitHub Trending のうち**ローカルLLM関連記事**の要約として `feed-local-llm.md` を生成する。
- `raw/reddit_local_llm.json` を読み、Reddit r/LocalLLaMA の投稿を**別セクション**の要約として掲載する。
- `feed-local-llm.md` は必ず全体を上書きする。
- fetch、commit、push、メール送信は行わない。

## 出力形式

```markdown
# GitHub Trending — ローカルLLM記事 全数要約 — YYYY-MM-DD

## ローカルLLM関連リポジトリ

| # | タイトル | 要約 | URL |
|---|----------|------|-----|
| 1 | <日本語見出し> | • 要点1<br>• 要点2<br>• 要点3<br><br>1段落で背景/影響/今後をまとめる。 | <一次ソース URL> |
```

続けて、Redditの記事がある場合は必ず次の別セクションを出力する。

```markdown
## Reddit r/LocalLLaMA（直近36時間）

| # | タイトル | 要約 | URL |
|---|----------|------|-----|
| 1 | <日本語見出し（原題併記可）> | • 要点1<br>• 要点2<br>• 要点3<br><br>背景/影響/今後を1段落でまとめる。 | <投稿URL> |
```

## セクションルール

- **全数掲載**: `raw/local_llm_github.json` に含まれる記事は**すべて**掲載する。件数で絞らない（「5〜8件選ぶ」等の制限は**適用しない**）。
- 記事が 0 件の場合は、見出しの代わりに「該当記事なし（今日の GitHub Trending にローカルLLM関連はありませんでした）」とだけ書く。
- 記事が 1 件の場合はテーブルではなく、その記事の要約だけを「## ローカルLLM関連リポジトリ」の下に箇条書きで書いてよい。
- 各記事は「日本語見出し」「要約(箇条書き3点+1段落)」「一次ソース URL」の順に書く。
- URL は各項目の末尾に単独行で置く。

### Redditセクション

- `raw/reddit_local_llm.json` に含まれる記事は**すべて**掲載する。取得スクリプトが直近36時間で絞り込んでいるため、さらに件数や人気度で絞らない。
- Redditの記事が 0 件の場合は「該当記事なし（直近36時間の r/LocalLLaMA 投稿はありませんでした）」とだけ書く。
- Redditのrawにはタイトル、プレーンテキスト化済み本文、投稿URL、投稿日時が含まれる。本文は要約の判断材料に使い、投稿日時を必要に応じて要約へ反映する。
- GitHub記事とReddit記事を同じテーブルや同じ項目として混在させない。

## 要約ルール

1. **タイトル**: 内容がわかる日本語見出しにする。英語タイトルは「<日本語意訳> — (原文: <English Title>)」形式で原題を併記。
2. **要約フォーマット** (重要): 必ず以下の構造にする:
   - 箇条書き 3 点 (各 1 行、`• ` で始める、改行は `<br>`)
   - 続いて 1 段落で背景・影響・今後をまとめる
   - 形式: `• 要点1<br>• 要点2<br>• 要点3<br><br>まとめの段落。`
3. **文字数**: 箇条書き 3 点 + 1 段落で合計 4〜5 文相当。
4. **一次ソース URL**: raw の URL を優先。
5. **客観的・中立**: 誇張、煽り、断定しすぎる表現を避ける。
6. **raw の `extra`**: 説明文などは要約の判断材料として使ってよい。
